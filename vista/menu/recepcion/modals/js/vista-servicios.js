//Estudios laboratorio clinico
setTimeout(() => {
    const button = document.querySelector('#select2-select-lab-container');
    const tooltip = document.querySelector('#tooltip');
    cargarServiciosEstudios(button, tooltip);
  }, 5000);

setTimeout(() => {
  const buttonIds = [`#select2-select-lab-container`];
  const tooltip = document.querySelector(`#tooltip`);
  cargarServiciosEstudios(buttonIds, tooltip);
}, 5000);

setTimeout(() => {
  const buttonIds = [`#select2-select-labbio-container`];
  const tooltip = document.querySelector(`#tooltip`);
  cargarServiciosEstudios(buttonIds, tooltip);
}, 5000);

setTimeout(() => {
  const buttonIds = [`#select2-select-rx-container`];
  const tooltip = document.querySelector(`#tooltip`);
  cargarServiciosEstudios(buttonIds, tooltip);
}, 5000);

setTimeout(() => {
  const buttonIds = [`#select2-select-us-container`];
  const tooltip = document.querySelector(`#tooltip`);
  cargarServiciosEstudios(buttonIds, tooltip);
}, 5000);




